# Porte 6F - Controlled Data Repair For Finalized Copy Without PDF

Date: 2026-06-24

## Context

Porte 6E deployed the copy integrity audit redaction fix.

Remaining bounded data issue:

- `FINALIZED_WITHOUT_FINAL_PDF_COUNT=1`
- `RELEASED_FINALIZED_WITHOUT_STUDENT_COUNT=0`
- affected technical copy id: `744cb7ed-bbfb-4109-b46d-f93d17002a03`
- affected technical exam id: `de96412e-8504-4929-abf1-89247df98775`

No student name, email, anonymous id, media path, or copy content is recorded here.

## Preflight

Production preflight:

- host: `korrigo`
- production health: `{"status":"healthy","database":"connected"}`
- backend/celery/celery-beat image: `korrigo-backend:korrigo-direct-c38a586`
- nginx image: `korrigo-nginx:korrigo-direct-f793f0c`
- DB/Redis: healthy

Server audit directory:

`/var/www/labomaths/korrigo_release/ops/porte6f_data_repair_20260624T122834Z`

Backup/sync guard before repair:

- latest encrypted backup: `20260624T101701Z`
- `db.sql.gz.gpg: OK`
- `media_inventory.txt.gpg: OK`
- `manifest.json: OK`
- `WOULD_TRANSFER_COUNT=0`
- `DELETE_COUNT=0`
- `ERROR_WORD_COUNT=0`

## Target Before Repair

Technical state before repair:

- `copy_pk=744cb7ed-bbfb-4109-b46d-f93d17002a03`
- `exam_pk=de96412e-8504-4929-abf1-89247df98775`
- `status=FINALIZED`
- `has_final_pdf=False`
- `final_pdf_name_present=False`
- `booklet_count=1`

Rollback snapshot:

- `status=FINALIZED`
- `final_pdf_name_present=False`
- `final_pdf_size_known=False`

Rollback note: if repair must be undone, unlink only the final PDF generated by this repair after explicit validation.

## Dry Run

Targeted dry-run command:

```bash
python manage.py check_copy_integrity \
  --copy-id 744cb7ed-bbfb-4109-b46d-f93d17002a03 \
  --fail-on-issues
```

Result:

- `TARGET_DRYRUN_RC=1`
- `EMAIL_COUNT=0`
- `STUDENT_EMAIL_KEY_COUNT=0`
- `ANONYMOUS_ID_KEY_COUNT=0`
- `FINALIZED_WITHOUT_FINAL_PDF_COUNT=2`
- `AT_SIGN_COUNT=0`

The non-zero return code was expected before repair.

## Repair

Targeted repair command:

```bash
python manage.py check_copy_integrity \
  --copy-id 744cb7ed-bbfb-4109-b46d-f93d17002a03 \
  --repair-missing-final-pdf \
  --fail-on-issues
```

The wrapper did not persist the command return-code file, so the repair was verified through the command output counters and the post-repair database state.

Repair output counters:

- `EMAIL_COUNT=0`
- `STUDENT_EMAIL_KEY_COUNT=0`
- `ANONYMOUS_ID_KEY_COUNT=0`
- `REPAIRED_COUNT=1`
- `REPAIR_FAILED_COUNT=0`
- `AT_SIGN_COUNT=0`

No batch repair was run. No SQL direct update was used.

## Target After Repair

Technical state after repair:

- `copy_pk=744cb7ed-bbfb-4109-b46d-f93d17002a03`
- `exam_pk=de96412e-8504-4929-abf1-89247df98775`
- `status=FINALIZED`
- `has_final_pdf=True`
- `final_pdf_name_present=True`
- `final_pdf_size_positive=True`

## Global Integrity Audit

Global audit after repair:

- `GLOBAL_AUDIT_AFTER_REPAIR_RC=0`
- `EMAIL_COUNT=0`
- `STUDENT_EMAIL_KEY_COUNT=0`
- `ANONYMOUS_ID_KEY_COUNT=0`
- `ISSUES_ZERO_COUNT=1`
- `FINALIZED_WITHOUT_FINAL_PDF_COUNT=0`
- `AT_SIGN_COUNT=0`

The copy integrity audit no longer reports an issue.

## Health And Logs

Health after repair:

`{"status":"healthy","database":"connected"}`

Recent logs on a short post-repair window:

| Service | Email count | `student_email` key count | `anonymous_id` key count | Error-like count | Warning-like count |
| --- | ---: | ---: | ---: | ---: | ---: |
| `docker-backend-1` | 0 | 0 | 0 | 0 | 0 |
| `docker-celery-1` | 0 | 0 | 0 | 0 | 0 |
| `docker-celery-beat-1` | 0 | 0 | 0 | 0 | 0 |
| `docker-nginx-1` | 0 | 0 | 0 | 0 | 0 |

The wider 10-minute log window still included pre-repair Celery errors. The short post-repair window was clean.

## Backup And Sync After Repair

Post-repair backup/sync check:

- latest encrypted backup: `20260624T101701Z`
- `db.sql.gz.gpg: OK`
- `media_inventory.txt.gpg: OK`
- `manifest.json: OK`
- `WOULD_TRANSFER_COUNT=0`
- `DELETE_COUNT=0`
- `ERROR_WORD_COUNT=0`

The latest existing encrypted backup is valid and synchronized. The next scheduled encrypted backup must be observed to confirm it captures the repair.

## Confirmations

- No GitHub, push, PR, workflow, GHCR, or registry was used.
- No build or application redeployment was performed in Porte 6F.
- No application restart was performed.
- No `docker compose up`, `docker compose down`, or `down -v` was run.
- No prune was run.
- No image, volume, or backup was deleted.
- No migration was run.
- No direct SQL was used.
- Only one technical copy id was repaired.
- No email, student name, anonymous id, media path, copy content, `.env`, secret, or pepper was displayed.

## Verdict

`PORTE6F_DATA_REPAIR_DONE`

The unique finalized copy without final PDF was repaired through the existing application command. The global integrity audit now passes with no issues.

## Next Step

Observe:

- the next scheduled copy integrity audit;
- the next encrypted backup and StorageBox sync after the repair.

Only after those observations are clean should strict Korrigo-only Docker cleanup be considered.
