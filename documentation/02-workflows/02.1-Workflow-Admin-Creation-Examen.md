# 02.1 - Workflow Complet : Création d'Examen par l'Administrateur

**Auteur** : Alaeddine BEN RHOUMA
**Date** : 20 Février 2026
**Version** : 2.0

---

## 🎯 Vue d'Ensemble

Ce workflow décrit **étape par étape** comment un administrateur crée un nouvel examen dans Korrigo, depuis la configuration initiale jusqu'au traitement des copies scannées.

**Durée totale** : 15-30 minutes (selon volume de scans)
**Prérequis** : Compte administrateur actif, fichiers préparés (CSV élèves, PDF scans)

---

## 📋 Prérequis et Préparation

### Fichiers Nécessaires

#### 1. Liste des Élèves (CSV)

**Format attendu** :
```csv
Élèves,Né(e) le,Adresse E-mail,Classe
DUPONT Jean,01/02/2008,jean.dupont@ecole.tn,T.01
MARTIN Marie,15/03/2008,marie.martin@ecole.tn,T.01
```

**Colonnes obligatoires** :
- `Élèves` : NOM Prénom (espace entre nom et prénom)
- `Né(e) le` : DD/MM/YYYY
- `Adresse E-mail` : email@domain.tn
- `Classe` : Code classe (optionnel)

**Validation** :
✓ Encodage : UTF-8
✓ Séparateur : virgule (,)
✓ Pas de lignes vides
✓ Emails uniques

#### 2. Scans des Copies (PDF)

**Formats supportés** :
- **A4** : 21 x 29.7 cm (595 x 842 pixels @ 72 DPI)
- **A3** : 29.7 x 42 cm (1190 x 1684 pixels @ 72 DPI)

**Organisation** :
- Un seul PDF contenant toutes les copies
- Pages dans l'ordre de scan
- Résolution minimale : 150 DPI (recommandé : 300 DPI)
- Noir et blanc ou couleur
- Taille max : 200 MB par fichier

**Cas A3 (CMEN Batch)** :
```
Format: 2 feuilles A3 recto-verso par élève = 4 pages A4

Organisation physique (scan):
Sheet 1 (A3):
  - Left half: Page 4
  - Right half: Page 1
Sheet 2 (A3):
  - Left half: Page 2
  - Right half: Page 3

Résultat attendu après traitement:
  Page 1, Page 2, Page 3, Page 4 (ordre logique)
```

---

## 🚀 Workflow Détaillé

### ÉTAPE 1 : Connexion Admin

```
URL: http://localhost:8088 (ou https://korrigo.labomths.tn)
Username: test_admin
Password: admin123
```

**Actions** :
1. Ouvrir navigateur
2. Aller sur URL Korrigo
3. Cliquer "Connexion"
4. Entrer identifiants admin
5. Cliquer "Se connecter"

**API Call** :
```http
POST /api/login/
Content-Type: application/json

{
  "username": "test_admin",
  "password": "admin123"
}

Response 200 OK:
{
  "message": "Login successful",
  "must_change_password": false
}

Cookies:
  - sessionid=<session_token>
  - csrftoken=<csrf_token>
```

**Vérifications** :
✓ Redirection vers dashboard
✓ Menu "Créer Examen" visible
✓ Rôle = "Admin" dans profil

---

### ÉTAPE 2 : Créer un Nouvel Examen

**Navigation** :
```
Dashboard → Menu "Examens" → Bouton "Créer un Examen"
```

**Formulaire de Création** :

#### 2.1 Informations Générales

| Champ | Type | Obligatoire | Exemple |
|-------|------|-------------|---------|
| **Nom de l'examen** | Texte | ✓ | "Bac Blanc Mathématiques 2026" |
| **Date** | Date picker | ✓ | 03/02/2026 |
| **Matière** | Select | ✓ | Mathématiques |
| **Niveau** | Select | ✓ | Terminale |
| **Durée** | Number | ✓ | 4 heures |
| **Coefficient** | Number | ✓ | 7 |

```javascript
// Frontend: ExamCreate.vue
const examData = {
  name: "Bac Blanc Mathématiques 2026",
  date: "2026-02-03",
  subject: "mathematics",
  level: "terminale",
  duration: 240,  // minutes
  coefficient: 7
}
```

#### 2.2 Structure de Notation (Barème)

**Définir les questions et sous-questions** :

```javascript
// Exemple de structure
gradingStructure: [
  {
    question: "1",
    title: "Étude de fonction",
    max_score: 5.0,
    sub_questions: [
      { id: "1.a", label: "Dérivée", max_score: 2.0 },
      { id: "1.b", label: "Tableau de variation", max_score: 2.0 },
      { id: "1.c", label: "Courbe", max_score: 1.0 }
    ]
  },
  {
    question: "2",
    title: "Probabilités",
    max_score: 4.0,
    sub_questions: [
      { id: "2.a", label: "Loi normale", max_score: 2.0 },
      { id: "2.b", label: "Intervalle de confiance", max_score: 2.0 }
    ]
  },
  {
    question: "3",
    title: "Géométrie dans l'espace",
    max_score: 6.0,
    // Sans sous-questions
  },
  {
    question: "4",
    title: "Algorithme",
    max_score: 5.0,
    sub_questions: [
      { id: "4.a", label: "Écriture algorithme", max_score: 3.0 },
      { id: "4.b", label: "Complexité", max_score: 2.0 }
    ]
  }
]

// Total: 5 + 4 + 6 + 5 = 20 points
```

**Interface** :
- Bouton "+ Ajouter Question"
- Pour chaque question :
  - Numéro (auto-incrémenté)
  - Titre
  - Score maximum
  - Option "+ Sous-question"
- Calcul automatique du total
- Validation : Total doit être cohérent (généralement 20 points)

#### 2.3 Sauvegarder l'Examen

**Bouton** : "Créer l'Examen"

**API Call** :
```http
POST /api/exams/
Content-Type: application/json
Authorization: Cookie sessionid=...

{
  "name": "Bac Blanc Mathématiques 2026",
  "date": "2026-02-03",
  "subject": "mathematics",
  "level": "terminale",
  "duration": 240,
  "coefficient": 7,
  "grading_structure": [
    {
      "question": "1",
      "title": "Étude de fonction",
      "max_score": 5.0,
      "sub_questions": [...]
    },
    ...
  ]
}

Response 201 Created:
{
  "id": "136db0b7-c496-4bd4-b662-d4dacee35ef4",
  "name": "Bac Blanc Mathématiques 2026",
  "date": "2026-02-03",
  "is_processed": false,
  "booklet_count": 0,
  "correctors": [],
  "created_at": "2026-02-03T09:00:00Z"
}
```

**Base de Données** :
```sql
-- Table: exams_exam
INSERT INTO exams_exam (
  id,
  name,
  date,
  subject,
  level,
  duration,
  coefficient,
  creator_id,
  is_processed,
  created_at
) VALUES (
  '136db0b7-c496-4bd4-b662-d4dacee35ef4',
  'Bac Blanc Mathématiques 2026',
  '2026-02-03',
  'mathematics',
  'terminale',
  240,
  7,
  15,  -- admin user_id
  false,
  NOW()
);

-- Table: exams_gradingstructure (une ligne par question)
INSERT INTO exams_gradingstructure (id, exam_id, question, title, max_score, sub_questions)
VALUES
  (1, '136db0b7...', '1', 'Étude de fonction', 5.0, '[{"id":"1.a","label":"Dérivée","max_score":2.0}...]'),
  (2, '136db0b7...', '2', 'Probabilités', 4.0, '[...]'),
  (3, '136db0b7...', '3', 'Géométrie dans l'espace', 6.0, NULL),
  (4, '136db0b7...', '4', 'Algorithme', 5.0, '[...]');
```

**Résultat** :
✓ Examen créé avec UUID unique
✓ Statut initial : `is_processed=False`
✓ Prêt pour upload des fichiers

---

### ÉTAPE 3 : Import Liste des Élèves (CSV)

**Navigation** :
```
Dashboard → Examen créé → Onglet "Élèves" → Bouton "Importer CSV"
```

**Actions** :
1. Cliquer "Parcourir" ou drag & drop
2. Sélectionner fichier CSV (UTF-8)
3. Prévisualiser (affichage des 5 premières lignes)
4. Valider et importer

**API Call** :
```http
POST /api/students/import/
Content-Type: multipart/form-data
Authorization: Cookie sessionid=...

Form Data:
  - file: students.csv (binary)
  - exam_id: 136db0b7-c496-4bd4-b662-d4dacee35ef4

Response 200 OK:
{
  "success": true,
  "imported": 50,
  "errors": [],
  "warnings": [
    "Ligne 12: email déjà existant, élève ignoré"
  ]
}
```

**Backend Processing** :
```python
# students/views.py - import_students()
def import_students(request):
    csv_file = request.FILES['file']
    exam_id = request.POST['exam_id']

    # Parse CSV
    df = pd.read_csv(csv_file, encoding='utf-8')

    # Validation
    required_columns = ['Élèves', 'Né(e) le', 'Adresse E-mail']
    for col in required_columns:
        if col not in df.columns:
            return Response({'error': f'Missing column {col}'}, status=400)

    # Import chaque ligne
    imported_count = 0
    for index, row in df.iterrows():
        # Parse nom/prénom
        full_name = row['Élèves'].strip()
        parts = full_name.split(' ', 1)
        last_name = parts[0]
        first_name = parts[1] if len(parts) > 1 else ''

        # Parse date
        dob_str = row['Né(e) le']
        dob = datetime.strptime(dob_str, '%d/%m/%Y').date()

        # Créer Student
        student, created = Student.objects.get_or_create(
            email=row['Adresse E-mail'],
            defaults={
                'first_name': first_name,
                'last_name': last_name,
                'date_of_birth': dob,
                'classe': row.get('Classe', '')
            }
        )

        if created:
            imported_count += 1

    return Response({
        'success': True,
        'imported': imported_count
    })
```

**Base de Données** :
```sql
-- Table: students_student
INSERT INTO students_student (email, first_name, last_name, date_of_birth, classe)
VALUES
  ('jean.dupont@ecole.tn', 'Jean', 'DUPONT', '2008-02-01', 'T.01'),
  ('marie.martin@ecole.tn', 'Marie', 'MARTIN', '2008-03-15', 'T.01'),
  ...
  -- Total: 50 élèves
```

**Résultat** :
✓ 50 élèves importés
✓ Visibles dans l'onglet "Élèves" de l'examen
✓ Whitelist prête pour OCR matching

---

### ÉTAPE 4 : Upload des Scans PDF

**Navigation** :
```
Dashboard → Examen → Onglet "Copies" → Bouton "Uploader Scans"
```

#### 4.1 Sélection du Fichier

**Interface** :
- Zone drag & drop
- Bouton "Parcourir"
- Barre de progression
- Estimation temps restant

**Actions** :
1. Sélectionner `scans_bac_blanc_2026.pdf` (120 MB, 200 pages)
2. Cocher "Mode Batch A3" (si applicable)
3. Cocher "Avec CSV élèves" (pour OCR auto)
4. Cliquer "Uploader et Traiter"

#### 4.2 Upload Progressif

**Frontend** :
```javascript
// Chunked upload pour gros fichiers
const uploadPDF = async (file) => {
  const chunkSize = 1024 * 1024 * 5; // 5 MB chunks
  const totalChunks = Math.ceil(file.size / chunkSize);

  for (let i = 0; i < totalChunks; i++) {
    const chunk = file.slice(
      i * chunkSize,
      Math.min((i + 1) * chunkSize, file.size)
    );

    const formData = new FormData();
    formData.append('chunk', chunk);
    formData.append('chunkIndex', i);
    formData.append('totalChunks', totalChunks);
    formData.append('filename', file.name);

    await axios.post('/api/exams/upload-chunk/', formData, {
      onUploadProgress: (progressEvent) => {
        const percentCompleted = Math.round(
          ((i + progressEvent.loaded / progressEvent.total) / totalChunks) * 100
        );
        updateProgress(percentCompleted);
      }
    });
  }

  // Finalize upload
  await axios.post('/api/exams/finalize-upload/', {
    exam_id: examId,
    filename: file.name,
    batch_mode: isBatchA3,
    with_csv: true
  });
};
```

**API Calls** :
```http
POST /api/exams/upload-chunk/
Content-Type: multipart/form-data

Form Data:
  - chunk: <binary>
  - chunkIndex: 0
  - totalChunks: 24
  - filename: scans_bac_blanc_2026.pdf

Response 200 OK:
{
  "chunk_received": 0,
  "total_chunks": 24
}

... (répété 24 fois) ...

POST /api/exams/finalize-upload/
Content-Type: application/json

{
  "exam_id": "136db0b7-c496-4bd4-b662-d4dacee35ef4",
  "filename": "scans_bac_blanc_2026.pdf",
  "batch_mode": true,
  "with_csv": true
}

Response 202 Accepted:
{
  "message": "Upload finalized, processing started",
  "task_id": "abc-123-def-456",
  "estimated_time": "5-10 minutes"
}
```

#### 4.3 Sauvegarde et Déclenchement du Traitement

**Backend** :
```python
# exams/views.py - finalize_upload()
@api_view(['POST'])
def finalize_upload(request):
    exam_id = request.data['exam_id']
    filename = request.data['filename']
    batch_mode = request.data.get('batch_mode', False)
    with_csv = request.data.get('with_csv', False)

    # Reconstituer le fichier à partir des chunks
    final_path = os.path.join(MEDIA_ROOT, 'exams', 'source', f'exam_{exam_id}.pdf')
    chunks_dir = os.path.join(TEMP_DIR, filename)

    with open(final_path, 'wb') as final_file:
        for i in range(total_chunks):
            chunk_path = os.path.join(chunks_dir, f'chunk_{i}')
            with open(chunk_path, 'rb') as chunk_file:
                final_file.write(chunk_file.read())
            os.remove(chunk_path)  # Cleanup

    # Sauvegarder dans DB
    exam = Exam.objects.get(id=exam_id)
    exam.pdf_source = final_path
    exam.save()

    # Déclencher traitement asynchrone (Celery)
    if batch_mode:
        task = process_batch_a3.delay(exam_id, with_csv)
    else:
        task = process_standard_pdf.delay(exam_id)

    return Response({
        'message': 'Upload finalized, processing started',
        'task_id': str(task.id),
        'estimated_time': '5-10 minutes'
    }, status=202)
```

**Stockage** :
```bash
/media/exams/source/exam_136db0b7-c496-4bd4-b662-d4dacee35ef4.pdf
  Size: 120 MB
  Pages: 200
  Format: A3
```

**Résultat** :
✓ PDF sauvegardé dans `/media/exams/source/`
✓ Tâche Celery lancée
✓ Interface affiche "Traitement en cours..."

---

### ÉTAPE 5 : Traitement Asynchrone (Celery)

**Task Celery** : `process_batch_a3.delay(exam_id, with_csv)`

#### 5.1 Détection Format

```python
# processing/services/batch_processor.py
def is_a3_format(pdf_path):
    doc = fitz.open(pdf_path)
    page = doc.load_page(0)
    width, height = page.rect.width, page.rect.height

    # A3: ~1190 x 1684 pixels (± 50 pixels de tolérance)
    is_a3 = (1140 < width < 1240) and (1634 < height < 1734)

    doc.close()
    return is_a3
```

**Résultat** :
✓ Format détecté : **A3**
✓ Algorithme : BatchA3Processor

#### 5.2 Split et Rotation des Pages A3

**Objectif** : Transformer chaque feuille A3 en 2 pages A4

```python
def _split_a3_to_a4(self, a3_image):
    """
    Split une image A3 en 2 moitiés A4 (gauche et droite).

    Input: a3_image (H x W x C) - ex: 1684 x 1190 pixels
    Output: (left_a4, right_a4) - chacune 1684 x 595 pixels
    """
    height, width = a3_image.shape[:2]
    mid_width = width // 2

    # Left half
    left = a3_image[:, :mid_width].copy()

    # Right half
    right = a3_image[:, mid_width:].copy()

    return left, right
```

**Traitement pour 88 pages A3** :
```
Page A3 #1 → [Page A4 #1-left, Page A4 #1-right]
Page A3 #2 → [Page A4 #2-left, Page A4 #2-right]
...
Page A3 #88 → [Page A4 #88-left, Page A4 #88-right]

Total: 88 pages A3 → 176 pages A4
```

#### 5.3 Réorganisation selon Organisation Physique

**Pour CMEN Batch** :

Chaque élève a 2 feuilles A3 recto-verso = 4 pages A3 scannées = 8 demi-pages

**Mapping physique → logique** :
```python
def _reorder_pages_per_sheet(self, pages_a4):
    """
    Réorganise les pages A4 selon l'ordre logique.

    Organisation physique (scan):
    A3#1-left  = Page 4
    A3#1-right = Page 1
    A3#2-left  = Page 2
    A3#2-right = Page 3

    Ordre logique souhaité: Page 1, 2, 3, 4
    """
    sheets = []
    for i in range(0, len(pages_a4), 4):  # 4 pages A3 = 1 copie
        sheet_pages = pages_a4[i:i+4]

        # Réorganisation
        reordered = [
            sheet_pages[1],  # A3#1-right = Page 1
            sheet_pages[2],  # A3#2-left  = Page 2
            sheet_pages[3],  # A3#2-right = Page 3
            sheet_pages[0],  # A3#1-left  = Page 4
        ]

        sheets.append(reordered)

    return sheets
```

**Résultat** :
✓ 88 pages A3 → 176 pages A4
✓ Réorganisées en 44 copies (4 pages par copie)

#### 5.4 Segmentation par Étudiant

**OCR du Header** :

```python
def _ocr_header(self, page_image):
    """
    Extrait nom/prénom/date depuis le header de la page.
    Utilise OCR GPT-4o-mini Vision pour robustesse.
    """
    # 1. Extraction de la zone header (200 premiers pixels)
    header_zone = page_image[:200, :]

    # 2. OCR GPT-4o-mini Vision
    ocr_engine = MultiLayerOCR()
    ocr_result = ocr_engine.extract_text_with_candidates(
        header_zone,
        csv_whitelist=self.students_whitelist
    )

    # 3. Matching avec CSV
    if ocr_result.top_candidates:
        top_match = ocr_result.top_candidates[0]
        if top_match.confidence > 0.7:
            # AUTO mode
            return top_match
        elif top_match.confidence > 0.4:
            # SEMI-AUTO mode (top 5 candidates)
            return ('SEMI_AUTO', ocr_result.top_candidates[:5])
        else:
            # MANUAL mode
            return ('MANUAL', None)

    return ('MANUAL', None)
```

**Pour chaque copie (4 pages)** :

```python
# Page 1 (première page de la copie)
ocr_result = _ocr_header(page1_image)

if ocr_result confidence > 0.7:
    # Attribution automatique
    student = Student.objects.get(email=ocr_result.email)
    copy = Copy.objects.create(
        exam_id=exam_id,
        student=student,
        is_identified=True,
        anonymous_id=generate_anonymous_id()
    )
else:
    # Marqué pour identification manuelle
    copy = Copy.objects.create(
        exam_id=exam_id,
        student=None,
        is_identified=False,
        needs_review=True,
        ocr_candidates=ocr_result.top_candidates  # Pour SEMI-AUTO
    )
```

**Résultat** :
✓ 44 copies créées
✓ ~30-35 copies identifiées automatiquement (AUTO)
✓ ~8-10 copies en mode SEMI-AUTO (top 5 candidats)
✓ ~1-4 copies en mode MANUAL

#### 5.5 Création des Booklets et Pages

```python
for copy_index, pages in enumerate(segmented_copies):
    # Créer Copy
    copy = Copy.objects.create(
        exam=exam,
        student=match.student if match else None,
        is_identified=match is not None,
        anonymous_id=generate_anonymous_id(),
        status='STAGING'  # En attente identification finale
    )

    # Créer Booklet (cahier)
    booklet = Booklet.objects.create(
        copy=copy,
        sequence=1
    )

    # Sauvegarder PDF du booklet
    booklet_pdf_path = f'/media/copies/booklets/copy_{copy.id}/booklet_1.pdf'
    save_images_as_pdf(pages, booklet_pdf_path)
    booklet.pdf_file = booklet_pdf_path
    booklet.save()

    # Créer Page entries
    for page_num, page_image in enumerate(pages, start=1):
        Page.objects.create(
            booklet=booklet,
            page_number=page_num,
            image_path=f'/media/exams/pages/copy_{copy.id}/page_{page_num}.png'
        )

        # Sauvegarder image
        cv2.imwrite(page.image_path, page_image)
```

**Base de Données** :
```sql
-- 44 copies créées
INSERT INTO grading_copy (id, exam_id, student_id, anonymous_id, is_identified, status)
VALUES
  ('uuid1', 'exam_id', 'student1_id', 'COPY-001', true, 'STAGING'),
  ('uuid2', 'exam_id', 'student2_id', 'COPY-002', true, 'STAGING'),
  ...
  ('uuid44', 'exam_id', NULL, 'COPY-044', false, 'STAGING');  -- Non identifié

-- 44 booklets
INSERT INTO grading_booklet (copy_id, sequence, pdf_file)
VALUES
  ('uuid1', 1, '/media/copies/booklets/copy_uuid1/booklet_1.pdf'),
  ...

-- 176 pages (44 copies × 4 pages)
INSERT INTO grading_page (booklet_id, page_number, image_path)
VALUES
  (booklet1_id, 1, '/media/exams/pages/copy_uuid1/page_1.png'),
  (booklet1_id, 2, '/media/exams/pages/copy_uuid1/page_2.png'),
  ...
```

**Stockage** :
```bash
/media/
├── exams/pages/
│   ├── copy_{uuid1}/
│   │   ├── page_1.png  (500 KB)
│   │   ├── page_2.png
│   │   ├── page_3.png
│   │   └── page_4.png
│   └── ... (44 copies)
│
└── copies/booklets/
    ├── copy_{uuid1}/
    │   └── booklet_1.pdf  (4 pages, 2 MB)
    └── ... (44 booklets)

Total: ~300 MB
```

**Résultat** :
✓ 44 copies créées
✓ 44 booklets générés
✓ 176 pages sauvegardées
✓ ~250-300 MB de stockage

---

### ÉTAPE 6 : Desk d'Identification (pour copies non identifiées)

**Navigation** :
```
Dashboard → Examen → Onglet "Identification" → "Desk d'Identification"
```

**Interface** :

```
┌──────────────────────────────────────────────────────────────┐
│  Desk d'Identification - Bac Blanc Maths 2026               │
│                                                              │
│  Copies à identifier: 12 / 44 (AUTO: 30, SEMI: 8, MANUAL: 4)│
└──────────────────────────────────────────────────────────────┘

┌─────────────────────┐  ┌──────────────────────────────────┐
│  Copie COPY-015     │  │  Suggestions OCR (SEMI-AUTO)     │
│  ┌───────────────┐  │  │                                  │
│  │               │  │  │  1. ✓ MARTIN Marie (78%)         │
│  │   [Preview]   │  │  │     Email: marie.martin@...      │
│  │   Page 1      │  │  │     DOB: 15/03/2008              │
│  │               │  │  │     [Sélectionner]               │
│  └───────────────┘  │  │                                  │
│                     │  │  2. MARTINEZ Maria (65%)          │
│  OCR détecté:       │  │     Email: maria.martinez@...    │
│  "MARTIN Marie"     │  │     [Sélectionner]               │
│  "15/03/2008"       │  │                                  │
│                     │  │  3. MARTINS Marie (52%)           │
└─────────────────────┘  │     [Sélectionner]               │
                         │                                  │
                         │  ─── Ou ───                      │
                         │                                  │
                         │  [Recherche Manuelle...]         │
                         └──────────────────────────────────┘
```

**Actions Admin** :

1. **SEMI-AUTO** : Cliquer sur candidat suggéré (ex: "MARTIN Marie 78%")
2. **MANUAL** : Recherche par nom/email, puis sélection

**API Call** :
```http
POST /api/identification/copies/{copy_id}/select-candidate/
Content-Type: application/json

{
  "rank": 1,  # ou student_id pour recherche manuelle
  "student_id": "student_uuid"
}

Response 200 OK:
{
  "success": true,
  "copy_id": "copy_uuid",
  "student": {
    "id": "student_uuid",
    "first_name": "Marie",
    "last_name": "MARTIN",
    "email": "marie.martin@ecole.tn"
  }
}
```

**Backend** :
```python
# identification/views.py
@api_view(['POST'])
def select_ocr_candidate(request, copy_id):
    copy = Copy.objects.get(id=copy_id)
    rank = request.data.get('rank')
    student_id = request.data.get('student_id')

    # Récupérer le candidat OCR sélectionné
    if rank:
        ocr_result = OCRResult.objects.get(copy=copy)
        candidate = ocr_result.top_candidates[rank - 1]
        student = Student.objects.get(id=candidate['student_id'])
    else:
        student = Student.objects.get(id=student_id)

    # Assigner l'étudiant à la copie
    copy.student = student
    copy.is_identified = True
    copy.status = 'READY'  # Prêt pour distribution
    copy.save()

    # Audit trail
    ocr_result.selected_candidate_rank = rank
    ocr_result.manual_override = (rank is None)
    ocr_result.save()

    return Response({'success': True, 'student': student.to_dict()})
```

**Base de Données** :
```sql
-- Update Copy
UPDATE grading_copy
SET
  student_id = 'student_uuid',
  is_identified = true,
  status = 'READY'
WHERE id = 'copy_uuid';

-- Audit OCRResult
UPDATE identification_ocrresult
SET
  selected_candidate_rank = 1,
  manual_override = false
WHERE copy_id = 'copy_uuid';
```

**Résultat** :
✓ Toutes les 44 copies identifiées
✓ Statut = 'READY'
✓ Prêt pour distribution aux correcteurs

---

### ÉTAPE 7 : Attribution aux Correcteurs

**Navigation** :
```
Dashboard → Examen → Onglet "Correcteurs" → "Distribuer les Copies"
```

**Interface** :

| Correcteur | Copies attribuées | Action |
|------------|-------------------|--------|
| Prof. DURAND | 0 / 44 | [Attribuer...] |
| Prof. LAMBERT | 0 / 44 | [Attribuer...] |
| Prof. ROUSSEAU | 0 / 44 | [Attribuer...] |

**Distribution** :

```javascript
// Option 1: Distribution automatique (équitable)
const distributeCopies = (copies, correctors) => {
  const copiesPerCorrector = Math.ceil(copies.length / correctors.length);

  correctors.forEach((corrector, index) => {
    const start = index * copiesPerCorrector;
    const end = Math.min(start + copiesPerCorrector, copies.length);
    const assignedCopies = copies.slice(start, end);

    assignedCopies.forEach(copy => {
      copy.corrector_id = corrector.id;
      copy.status = 'ASSIGNED';
    });
  });
};

// Option 2: Attribution manuelle
// Admin sélectionne copies et correcteur
```

**API Call** :
```http
POST /api/exams/{exam_id}/distribute-copies/
Content-Type: application/json

{
  "distribution": [
    {
      "corrector_id": "prof_durand_id",
      "copy_ids": ["copy_uuid1", "copy_uuid2", ..., "copy_uuid15"]
    },
    {
      "corrector_id": "prof_lambert_id",
      "copy_ids": ["copy_uuid16", ..., "copy_uuid30"]
    },
    {
      "corrector_id": "prof_rousseau_id",
      "copy_ids": ["copy_uuid31", ..., "copy_uuid44"]
    }
  ]
}

Response 200 OK:
{
  "success": true,
  "distributed": 44,
  "correctors": 3
}
```

**Base de Données** :
```sql
-- Update copies avec corrector_id
UPDATE grading_copy
SET
  corrector_id = 'prof_durand_id',
  status = 'ASSIGNED'
WHERE id IN ('copy_uuid1', 'copy_uuid2', ...);

-- Notification aux correcteurs (via Celery)
INSERT INTO notifications (user_id, message, type, created_at)
VALUES
  ('prof_durand_id', '15 copies assignées pour Bac Blanc Maths', 'ASSIGNMENT', NOW()),
  ...
```

**Résultat** :
✓ 44 copies distribuées à 3 correcteurs (~15 chacun)
✓ Notifications envoyées
✓ Correcteurs peuvent commencer la correction

---

## 📊 Résumé et Statistiques

### Temps de Traitement

| Étape | Durée | Status |
|-------|-------|--------|
| **Création examen** | 5 min | ✅ Complété |
| **Import CSV (50 élèves)** | 30 sec | ✅ Complété |
| **Upload PDF (120 MB)** | 2 min | ✅ Complété |
| **Traitement batch (88 A3 → 44 copies)** | 8-10 min | ✅ Complété |
| **Identification desk (12 copies)** | 5-10 min | ✅ Complété |
| **Distribution correcteurs** | 1 min | ✅ Complété |
| **TOTAL** | **22-30 min** | ✅ **Prêt pour correction** |

### État Final

**Examen** :
- ID: 136db0b7-c496-4bd4-b662-d4dacee35ef4
- Nom: Bac Blanc Mathématiques 2026
- Date: 03/02/2026
- Statut: `is_processed=True`

**Copies** :
- Total: 44 copies
- Identifiées: 44 (100%)
- Distribuées: 44 (100%)
- Statut: `ASSIGNED`

**Stockage** :
- PDF source: 120 MB
- Pages extraites: 88 MB
- Booklets: 176 MB
- **Total: ~380 MB**

**Prochaine étape** :
→ Voir [02.6-Workflow-Correction.md](./02.6-Workflow-Correction.md) pour le workflow de correction par les enseignants.

---

**Document rédigé par :**
**Alaeddine BEN RHOUMA**
*Lead Senior Documentation & Architecture*
Date : 20 Février 2026
